// The address of your API.
//
// 1. Deploy the server/ folder to Render.
// 2. Paste the Render URL below (no trailing slash), e.g.
//      https://business-directory-api.onrender.com
// 3. Commit and push. Netlify redeploys the site automatically.
//
// When you open the site from localhost (or a home-network address like 192.168.x.x),
// it talks to your local API on port 5000 instead, so you don't need to change
// this while developing.

window.APP_CONFIG = {
  API_URL: "https://YOUR-API-NAME.onrender.com",
  LOCAL_API_PORT: 5000,
};
